//buffer => Representação de um espaço na memória do computador;
// Itens que serão armazenados e depois removidos;
// Buffer é uma API criada dentro do node para trabalhar com dados binários;

const buf = Buffer.from("Olá")

console.log(buf)